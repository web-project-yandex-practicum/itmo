const headerMenuButton = document.querySelector(".header__menu-button");
const headerMenuList = document.querySelector(".header__menu-list");
const projectCards = document.querySelector(".projects__cards");
const paginationNumbersList = Array.from(
  document.querySelectorAll(".projects__pagination-number")
);

export {
  headerMenuButton,
  headerMenuList,
  projectCards,
  paginationNumbersList,
};
